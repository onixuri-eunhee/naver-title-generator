# Override site URL for OG/metadata in dev and preview deployments
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# OpenAI API Key — Whisper 전사 (Phase F 음성 업로드) + GPT Image 레거시 fallback
OPENAI_API_KEY=sk-...
