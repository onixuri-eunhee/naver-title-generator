# 숏폼 대본 톤 개인화 — 디자인

- 작성일: 2026-04-11
- 영향 도구: shortform.html, blog-writer.html, api/shortform-script.js
- 관련 메모: project_session_0411.md (4/11 ElevenLabs 푸시 직후 후속 작업)

## 배경

현재 숏폼 대본 생성 결과는 후킹(Hook) 섹션은 잘 작동하지만 본문(Point)과 마무리(CTA)가 "나열 정리체" 느낌이 강하다. 운영자 피드백:

> 본문과 CTA만 조금 더 사람 느낌으로 경험의 예시를 담으면 충분할 것 같아. 현재는 그냥 나열 정리만 하는 느낌이라서.

`api/shortform-script.js`의 현재 프롬프트 분석 결과 진단:

- **Point 섹션 (line 75)**: `"나열 금지, 구체적 사례와 숫자로 깊이 있게"` — 추상적이라 AI가 결국 나열로 회귀
- **CTA 섹션 (line 76-81)**: 4종 정형 문구(`"궁금한 점 댓글로 남겨주세요"`, `"유용했다면 팔로우 부탁드려요"` 등) 중 선택. 정확히 "정리체"의 원흉

블로그 글 작성기(blog-writer.html)는 이미 `#memo` 필드로 운영자 페르소나/경험을 받아 1인칭 톤으로 가공하는 검증된 패턴을 갖고 있다. 같은 패턴을 숏폼에 이식한다.

## 목표

운영자의 페르소나/경험을 메모로 받아 AI가 숏폼 톤(28자/씬)으로 압축해 본문(Point)과 마무리(CTA)에 자연스럽게 녹인다. 메모가 없어도 기존 정형 문구를 폐기하고 동료 호출형 톤으로 대체한다.

## 입력 경로

두 가지 진입 경로가 있다. 두 경로 모두 `api/shortform-script.js` request body의 신규 필드 `personaMemo`로 통합 전송된다. AI는 메모 길이와 무관하게 동일한 방식으로 가공한다.

### 경로 A: blog-writer 경유

기존 `blogTextForImagePro` 핸드오프 패턴을 복제한다.

- **송신**: blog-writer.html에서 "숏폼 만들기" 버튼 클릭 시 `localStorage.setItem('blogTextForShortform', JSON.stringify({body, memo}))` 후 shortform.html로 이동
- **수신**: shortform.html `DOMContentLoaded`에서 키 존재 여부 확인 → 있으면 `topic`/`blogText`/`personaMemo` 필드에 자동 채움 → 키 삭제(1회성)
- 메모는 블로그 작성기의 긴 단락 그대로 (수십 줄 가능)

### 경로 B: 직접 진입

shortform.html에 신규 optional textarea 추가.

- **위치**: 기존 topic 입력 필드 아래
- **레이블**: "내 경험 한 줄 (선택)"
- **placeholder**: "본인의 경험·페르소나·실패담을 한 줄로 적어주세요. 비워도 됩니다.\n예: 15년차 헤어 디자이너, 처음엔 반신반의했는데 한 달 뒤 머릿결이 확 달라짐"
- **저장 동작**: 마지막 입력 자동 저장(`localStorage.shortformLastMemo`) → 다음 진입 시 자동 채움 (운영자가 매번 같은 페르소나 다시 입력하는 마찰 제거)
- **검증**: 길이 제한 없음, optional

## 프롬프트 변경 (`api/shortform-script.js`)

### Point 섹션 (현재 line 75 교체)

```
- Point(핵심 3~5씬): 하나의 핵심 메시지에 집중. 추상적 나열 금지.
  ★ 메모(personaMemo)가 있으면: Point 중 최소 1씬은 메모의 경험을 28자 압축 인용.
    원문 그대로 X — 핵심 감정/장면/숫자만 추출해 숏폼 톤으로 재구성.
    예) 메모 "15년차 헤어 디자이너, 손님이 '여기 물 맛있다'고 했을 때 뿌듯했음"
        → 씬 "손님이 '물 맛있다' 한마디에 울컥했어요"
  ★ 메모가 없으면: 관찰형 1인칭 허용 — "~인 분들 많으세요", "저도 처음엔 ~인 줄 알았는데"
  ★ 나열 vs 경험 예시 (반드시 후자):
    ❌ "키워드 분석, 카테고리 설정, 콘텐츠 발행이 중요합니다"
    ⭕ "어제 알림 50개 떴어요"
    ❌ "꾸준한 노력이 필요합니다"
    ⭕ "6개월간 매일 쓰다가 한 달 쉬었더니 다 무너졌어요"
```

### CTA 섹션 (현재 line 76-81 전면 교체)

```
- CTA(마지막 1~2씬): 정형 문구 금지. 시청자를 동료로 호출.
  ★ 금지: "궁금한 점 댓글로 남겨주세요", "유용했다면 팔로우 부탁드려요",
    "도움 됐으면 좋아요", "나중에 다시 보려면 저장해두세요", "감사합니다"
  ★ 허용 톤 예시:
    - "혹시 비슷한 경험 있으면 댓글로 알려주세요"
    - "다음 영상에서 더 깊이 다뤄볼까요?"
    - 메모 있을 때: "저처럼 [메모 핵심 키워드]로 고민이라면 댓글 남겨주세요"
  ★ CTA도 28자 제약 동일 적용
```

### userPrompt 변경 (`buildUserPrompt`, line 111~)

- `inputSummary`에 `personaMemo: ${personaMemo || '(없음)'}` 라인 추가
- 본문 마지막에 한 줄 추가:
  `"personaMemo가 있으면 Point 중 최소 1씬에 가공해 녹이세요. 없으면 관찰형 1인칭으로."`

### 출력 스키마

**변경 없음**. 기존 `scenes[].script / section / type / visual / hookText` 그대로 유지. 톤만 바뀌므로 다운스트림(자막, B-roll 등)은 영향 없음.

## 안전망

운영자가 결과 톤이 마음에 안 들면 **shortform.html의 기존 자막/대본 편집 기능으로 직접 수정 가능**. 톤을 공격적으로 가도 후수정으로 회복 가능하므로 정형 문구 폴백을 두지 않는다.

## 영향 파일

| 파일 | 변경 규모 | 변경 내용 |
|---|---|---|
| `blog-writer.html` | ~20 lines | "숏폼 만들기" 버튼 + `blogTextForShortform` 송신 |
| `shortform.html` | ~30 lines | 핸드오프 수신 + `personaMemo` textarea 추가 + `lastMemo` 자동 채움 |
| `api/shortform-script.js` | ~40 lines diff | request body에 `personaMemo` 추출, Point/CTA 프롬프트 재작성, userPrompt에 메모 라인 추가 |

## Out of Scope (의도적 제외)

- 자막 편집기 UI 확장 (폰트/색상/외곽선 등 fingr 벤치마크 기능) — 별도 작업
- 후킹(Hook) 섹션 변경 — 현재 잘 작동 중
- blog-writer 측 "숏폼 만들기" 버튼 정확한 배치 디테일 — 구현 단계에서 결정
- 메모 내용 검증/필터링 (욕설, 광고 등) — 1차에선 운영자 신뢰
- 페르소나 vs 경험 분리 (페르소나 = 영구 저장, 경험 = 1회성) — 단일 필드 + 자동 저장으로 통합

## 리스크

1. **AI 가공 정확도**: 메모 압축 시 운영자 의도와 다른 사례 만들 수 있음 → 후수정 흡수
2. **메모 없는 케이스 어색함**: 보수적 톤을 원하는 일부 사용자에겐 동료 호출형 CTA가 어색 가능 → 후수정 가능
3. **localStorage 키 충돌**: `blogTextForShortform`이 기존 키와 겹치지 않는지 확인 필요 (현재 grep으로 미존재 확인됨)
4. **개인정보**: 메모에 운영자 본명/사업명 들어갈 수 있음 → 1차에선 운영자가 본인 책임으로 입력 (저장은 localStorage 한정, 서버 영구 저장 없음)

## 검증 방법

1. 수동 비교: 동일 토픽으로 (메모 있음 / 메모 없음) 각 1회 생성 → 톤 차이 운영자 육안 확인
2. CTA 정형 문구 금지 검증: 생성 결과에 `"궁금한 점 댓글로"`, `"감사합니다"` 등 폐기 문구가 0회 등장하는지 확인
3. blog-writer → shortform 핸드오프 E2E: blog-writer에서 글 작성 → 숏폼 만들기 클릭 → shortform.html에서 자동 채워졌는지 확인
